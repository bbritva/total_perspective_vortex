# Minor Issues Fix — TODO

- [x]   1. `requirements.txt` — Remove unused `pandas>=1.5.0` dependency
- [x]   2. `README.md` — Remove references to `visualization.py` and `test.py` from project structure
- [x]   3. `TASKS.md` — Update task statuses to reflect actual completion (Phases 3–6)

# Streaming & Latency Fix — TODO

- [x]   4. `pipeline.py` — Add 2s latency warning to `predict_stream()`
- [x]   5. `mybci.py` — Add `predict_raw` CLI mode using `predict_raw_stream()`
- [x]   6. `README.md` — Document the new `predict_raw` mode
